var age = 39;
if (age >= 18) {
  goVote();
}

// abstraction: case that we only want to do one thing if some condition

if (isEnrolled()) {
  takeClass();
}
else {
  enrollFirst();
}

// abstraction: make a decisions between 2 different things
